<view
  class="piece {{color}} {{selected ? 'selected' : ''}}"
  bindtap="onTap"
>
  <image 
    wx:if="{{type}}" 
    class="piece-icon" 
    src="/program/assets(sucaiku)/pieces_icon/{{color}}_{{type}}.png" 
    mode="aspectFit"
  />
</view>